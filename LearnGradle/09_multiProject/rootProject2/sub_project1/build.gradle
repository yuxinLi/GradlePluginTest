
task taskA << {
    println " this is ${name} form ${project.name}"
}

task taskB << {
    print "this is ${name} form ${project.name}"
}

taskA.dependsOn taskB
taskB.dependsOn ':sub_project2:taskC'
